в файле native-lib.cpp представлено 2 метода:
1-ый принимает на вход jstring в котором передаётся путь к файлу, затем преобразует этот путь в std::string, после чего возвращает содержимое файла
2-ой получает на вход содержимое и путь, куда будет сохранён файл. Вызывается в MainActivity.kt:26 с переданным в него getExternalFilesDir.absolutePath

В /src/build.gradle.kts на строчке 20 добавлена следуюшая строка:
abiFilters.add("arm64-v8a"), ограничивающая используемый ABI, другого способа не использовал, т.к. конфигурация gradle показалась самой простой
